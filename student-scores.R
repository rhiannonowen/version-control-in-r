# Create a vector called scores
Scores<- c(65,68,70,70,71)
# Create a character vector called Names
Names <- c("Tayo", "Chance", "Seyi", "Louis", "Phil")
length(Names)
# Create a character vector called Gender
Gender <- c("F", "M", "F", "M", "M")
length(Gender)
# Use the data.frame function to create a data frame
stuMarks <- data.frame(Names, Scores, Gender)
stuMarks